name: Build, Publish and Release Teraslice Asset
run-name: ${{ github.actor }} is building, publishing and releasing the Teraslice Asset
on:
  pull_request:
    branches:
      - master
      - release/standard-assets-v*
    types:
      - closed

permissions: {}

jobs:
  call-asset-build:
    if: github.event.pull_request.merged == true
    uses: terascope/workflows/.github/workflows/asset-build-and-publish.yml@2e12a5feded0f4ff6fc3143a0abf088029bcc05f
    with:
      teraslice-cli-version: ${{ startsWith(github.base_ref, 'release/standard-assets-v') && 'prerelease' || 'latest' }}
    permissions:
      id-token: write  # Required for OIDC
      contents: read
      pages: write     # to deploy to Pages
    secrets:
      RELEASES_PRIVATE_KEY: ${{ secrets.RELEASES_PRIVATE_KEY }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
